<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/bootstrap/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/bootstrap-icons/font/bootstrap-icons.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Chi tiết</title>
</head>

<body>
    <nav class="navbar navbar-expand-sm navbar-dark bg-dark">
        <div class="container-fluid">
            <div class="collapse navbar-collapse" id="mynavbar">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Sản Phẩm</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/cart">Giỏ Hàng</a>
                    </li>
                    <% if (loggedin) { %>
                        <% if (loggedin) { %>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="/admin" role="button" data-bs-toggle="dropdown">Quản Trị Viên</a>
                                <ul class="dropdown-menu">
                                  <li><a class="dropdown-item" href="/admin/order">Đơn Hàng</a></li>
                                  <li><a class="dropdown-item" href="/admin/laptop">Sản Phẩm</a></li>
                                </ul>
                              </li>
                            <% } %>
                        <% } %>
                </ul>
                <form class="d-flex">
                    <input class="form-control me-2" type="text" placeholder="Nhập tên sản phẩm">
                    <button class="btn btn-primary" type="button"><i class="bi bi-search"></i></button>
                </form>
                <div style="margin-left: 10px; margin-right: 10px;">
                    <% if (loggedin) { %>
                        <form method="post" action="/auth/logout">
                            <button class="btn btn-danger" type="submit">
                                Đăng xuất
                            </button>
                        </form>
                        <% } else { %>
                            <form method="get" action="/auth">
                                <button class="btn btn-primary" type="submit">
                                    Đăng nhập
                                </button>
                            </form>
                            <% } %>
                </div>
            </div>
        </div>
    </nav>

    <div class="row">
        <div class="col-md-8 offset-2">
            <h4 class="text-center">Chi tiết Laptop</h4>
            <div class="row">
                <div class="text-danger"></div>
                <div class="col">
                    <div class="col-md-6" style="width: 100%;">
                        <div class="form-group">
                            <label class="control-label mt-3 mb-3" for="name">Tên:</label>
                            <input type="text" class="form-control" name="name" value="<%= laptop[0].name %>"
                                readonly />
                            <span class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label class="control-label mt-3 mb-3" for="price">Giá:</label>
                            <input type="number" class="form-control" name="price" value="<%= laptop[0].price %>"
                                readonly />
                            <span class="text-danger"></span>
                        </div>
                        <% if (loggedin) { %>
                            <div class="form-group">
                                <label class="control-label mt-3 mb-3" for="image">Đường dẫn ảnh:</label>
                                <input type="text" class="form-control" name="image" value="<%= laptop[0].img %>"
                                    readonly />
                                <span class="text-danger"></span>
                            </div>
                            <% } %>
                    </div>
                    <div class="col-md-12">
                        <div class="form-group">
                            <label class="control-label mt-3 mb-3" for="description">Mô tả:</label>
                            <textarea class="form-control" name="description" style="height: 200px"
                                readonly><%= laptop[0].description %></textarea>
                            <span class="text-danger"></span>
                        </div>
                    </div>
                    <!-- <div class="col-md-12">
                            <div class="form-group">
                                <label class="mt-3 mb-3" for="Status">Tình trạng:</label>
                                <select class="form-control mb-3" id="Status" name="Status">
                                    <option value="1">Còn hàng</option>
                                    <option value="0">Hết hàng</option>
                                </select>
                                <span class="text-danger"></span>
                            </div>
                        </div> -->
                </div>
                <p></p>

                <div class="form-group d-flex justify-content-between">
                    <a class="btn btn-outline-secondary" href="/">Xem tất cả</a>
                    <% if (laptop[0].status==1) { %>
                        <form method="post" action="/cart/add" class="d-flex justify-content-end">
                            <div class="mt-5">
                                <div class="input-group">
                                    <input type="hidden" name="laptopId" value="<%= laptop[0].id %>">
                                    <input type="hidden" name="laptopName" value="<%= laptop[0].name %>">
                                    <input type="hidden" name="laptopPrice" value="<%= laptop[0].price %>">
                                    <input type="hidden" name="laptopAmount" value="1">
                                </div>
                            </div>
                            <% if (!loggedin) { %>
                            <button class="btn btn-success" type="submit">
                                Thêm vào giỏ hàng
                            </button>
                            <% } %>
                        </form>
                        <% } %>
                </div>

            </div>
        </div>
    </div>

</body>

</html>